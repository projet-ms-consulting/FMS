{% extends 'dashboard_layout.html.twig' %}

{% block content %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Créer</button>
    {{ form_end(form) }}
    <a href="{{ path('address_index') }}">Annuler</a>

{% endblock %}

