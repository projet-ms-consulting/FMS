<tr>
    {% for column in columns %}
    <th scope="col"
        class="py-3.5 {{ loop.first ? 'pl-6 pr-3' : 'px-3' }} text-left text-sm font-semibold text-gray-900">
        {{ column }}
    </th>
    {% endfor %}
</tr>